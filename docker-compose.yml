services:
  app-dev:
    build: 
      context: ./app
      target: dev
    ports:
      - "8080:5173"
    volumes:
      - ./app:/app
      - /app/node_modules
    depends_on:
      - pocketbase
    networks:
      - mealnet
    profiles: ["dev"]

  app-prod:
    build:
      context: ./app
      target: prod
    ports:
      - "8080:80"
    depends_on:
      - pocketbase
    networks:
      - mealnet
    profiles: ["prod"]

  pocketbase:
    build: ./pb
    restart: unless-stopped
    volumes:
      - ./pb_data:/pb/pb_data
      - ./pb/pb_migrations:/pb/pb_migrations
    networks:
      - mealnet

networks:
  mealnet:
